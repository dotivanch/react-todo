{
  "dependencies": {
    "axios": "^0.19.1",
    "bcrypt": "^3.0.7",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.8.9",
    "pm2": "^4.2.1"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^2.0.2",
    "supertest": "^4.0.2",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "start": "export $(cat .env | xargs) && node src/loader.js",
    "dev": "export $(cat .env | xargs) && nodemon src/loader.js",
    "test": "export $(cat .env | xargs) && jest",
    "production": "export $(cat .env | xargs) && pm2 start src/loader.js --name todo-backend --no-daemon"
  },
  "name": "backend",
  "version": "1.0.0",
  "main": "src/loader.js",
  "license": "MIT"
}
